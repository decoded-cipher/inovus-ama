{
  "name": "inobot-server",
  "version": "1.0.0",
  "description": "Server for Inobot, a generative AI chatbot",
  "author": "Arjun Krishna",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "hono": "^4.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
